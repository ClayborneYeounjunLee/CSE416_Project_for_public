# Copy this file to `.env` and fill in your own values. Never commit `.env`.

# MongoDB connection string (e.g. MongoDB Atlas). Local fallback example:
# MONGODB_URI=mongodb://localhost:27017/cse416
MONGODB_URI=

# OpenAI API key — https://platform.openai.com/api-keys
OPENAI_API_KEY=
